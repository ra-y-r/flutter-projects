import 'package:flutter/material.dart';
import 'package:vend/constants.dart';

class RoundedListItem extends StatelessWidget {
  const RoundedListItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Row(
        children: [],
      ),
    );
  }
}
